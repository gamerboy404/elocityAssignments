import { Node, LinkedList } from "../../src/LinkedList/linkedList.js";

describe("a Class that creates a a linked list data type", () => {
  it("test 1 insert at head", () => {
    //expect(([2, 1])).toEqual([1, 2]);
  });
});